{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend server",
  "main": "index.js",
  "scripts": {
    "backend": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
